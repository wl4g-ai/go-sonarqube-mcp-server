name: Shadow scans
on:
  schedule:
    # Run the workflow every day at 04:00 UTC
    - cron: '0 4 * * *'
  workflow_dispatch:

jobs:
  scan:
    runs-on: github-ubuntu-latest-s
    name: Scan on shadow platforms
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
        with:
          version: 2026.6.1
      - uses: SonarSource/ci-github-actions/build-gradle@7f74370627fe3903b9fca9324786313d7b17d799 # 1.7.0
        with:
          run-shadow-scans: true
          artifactory-reader-role: private-reader
          artifactory-deployer-role: qa-deployer
          gradle-args: ":jacocoTestReport"
      - name: Run IRIS Analysis
        uses: SonarSource/unified-dogfooding-actions/run-iris@03e023516511b1c9da325ad104c2290e3b91688c # 1.1.0
        with:
          primary_project_key: "SonarSource_sonarqube-mcp-server"
          primary_platform: "SQC-EU"
          shadow1_project_key: "SonarSource_sonarqube-mcp-server"
          shadow1_platform: "Next"
          shadow2_project_key: "SonarSource_sonarqube-mcp-server"
          shadow2_platform: "SQC-US"
